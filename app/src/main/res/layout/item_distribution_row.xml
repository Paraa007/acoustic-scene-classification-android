<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingVertical="6dp">

    <!-- Column 1: Emoji (fixed width, centered) -->
    <TextView
        android:id="@+id/emojiText"
        android:layout_width="32dp"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:gravity="center"
        android:layout_marginEnd="8dp" />

    <!-- Column 2: Class Name + Count -->
    <TextView
        android:id="@+id/classNameText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textColor="@color/text_primary"
        android:textSize="14sp"
        android:ellipsize="end"
        android:maxLines="1" />

    <!-- Column 3: Count Badge -->
    <TextView
        android:id="@+id/countBadge"
        android:layout_width="36dp"
        android:layout_height="24dp"
        android:gravity="center"
        android:textColor="@color/text_primary"
        android:textSize="12sp"
        android:textStyle="bold"
        android:background="@drawable/badge_background"
        android:layout_marginStart="8dp" />

    <!-- Column 4: Progress Bar -->
    <ProgressBar
        android:id="@+id/progressBar"
        style="@style/Widget.AppCompat.ProgressBar.Horizontal"
        android:layout_width="60dp"
        android:layout_height="8dp"
        android:layout_marginStart="8dp"
        android:max="100"
        android:progressDrawable="@drawable/progress_bar_distribution" />

</LinearLayout>
